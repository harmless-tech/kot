mod lexer;
mod writer;
mod tokens;

pub fn compile(/* List of files */ /* Build folder */ /* Compress into zip */ /* Clean build */ /* Debug */
) {
}

pub fn build(/* Build file */ /* Debug */) {}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
