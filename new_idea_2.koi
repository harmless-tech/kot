const upper_koi: mod = use "new_idea.koi";

const TestObject: type = upper_koi::TestObject;

type NotTestObject = upper_koi::TestObject;

// This will add - to TestObject for those who use it through this module.
def -(o1, o2): upper_koi::TestObject {
    a - b
}
