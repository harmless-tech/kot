kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: amd64

trigger:
  branch:
    - main
  event:
    - push

steps:
- name: test
  image: rust:latest
  commands:
    - cd impls/rust
    - cargo test --workspace
    - cargo test --workspace --release
- name: build
  image: rust:latest
  commands:
    - cd impls/rust
    - cargo build --workspace
    - cargo build --workspace --release
